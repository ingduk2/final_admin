<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="main">
    
    <select id="mainmembercnt" resultType="int">
    	select count(mid) from member
    </select>
    <select id="mainboardcnt" resultType="int">
    	select count(bno) from board
    </select>
    <select id="mainanimalcnt" resultType="int">
    	select count(anino) from animal
    </select>
    
    <!-- select count(*) ,anistate, to_char(anidate,'yyyy-mm') from animal where anino!=0 group by anistate, to_char(anidate,'yyyy-mm') 
having anistate=0 order by 3 asc;

select count(*) ,anistate, to_char(anidate,'yyyy-mm') from animal where anino!=0 group by anistate, to_char(anidate,'yyyy-mm') 
having anistate=3 order by 3 asc;

select count(*) ,anistate, to_char(anidate,'yyyy-mm') from animal where anino!=0 group by anistate, to_char(anidate,'yyyy-mm') 
having anistate=4 order by 3 asc;

select count(*) ,anistate, to_char(anidate,'yyyy-mm') from animal where anino!=0 group by anistate, to_char(anidate,'yyyy-mm') 
having anistate=5 order by 3 asc; -->
    <select id="month0" resultType="monthvo">
    	select count(*) cnt ,anistate, nvl(to_char(anidate,'yyyy-mm'),'1') anidate from animal where anino!=0 group by anistate, to_char(anidate,'yyyy-mm') 
having anistate=0 order by 3 asc
    </select>
    
    <select id="month3" resultType="monthvo">
    select count(*) cnt, anistate, nvl(to_char(anidate,'yyyy-mm'),'1') anidate from animal where anino!=0 group by anistate, to_char(anidate,'yyyy-mm') 
having anistate=3 order by 3 asc
    </select>
    
    <select id="month4" resultType="monthvo">
    select count(*) cnt, anistate, nvl(to_char(anidate,'yyyy-mm'),'1') from animal where anino!=0 group by anistate, to_char(anidate,'yyyy-mm') 
having anistate=4 order by 3 asc
    </select>
    
    <select id="month5" resultType="monthvo">
    select count(*) cnt, anistate, nvl(to_char(anidate,'yyyy-mm'),'1') from animal where anino!=0 group by anistate, to_char(anidate,'yyyy-mm') 
having anistate=5 order by 3 asc
    </select>
    
    </mapper>